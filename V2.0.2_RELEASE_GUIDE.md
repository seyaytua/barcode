# 🚀 v2.0.2 リリース作成ガイド（超簡単！）

## ✅ 準備完了！

すべてのファイルがGitHubにプッシュされました：
- ✅ version.py → 2.0.2
- ✅ version_info_v2.txt → 2.0.2
- ✅ barcode_v2.spec → 2.0.2
- ✅ core/qrcode_generator.py（欠落ファイルを修正）
- ✅ RELEASE_v2.0.2.md

---

## 📋 実施手順（たった2ステップ！）

### ステップ1️⃣: GitHub Actionsのworkflowを更新（5分）

**workflowはすでにほぼ更新済みです！** あと少しだけ修正が必要です。

1. **以下のURLをブラウザで開く：**
   ```
   https://github.com/seyaytua/barcode/edit/main/.github/workflows/build.yml
   ```

2. **「Create ZIP archive」セクション（50行目付近）を以下に置き換え：**

   **現在のコード:**
   ```yaml
   - name: Create ZIP archive
     run: |
       Compress-Archive -Path dist/BarcodeQRCodePDF_v2.exe, dist/README.txt -DestinationPath BarcodeQRCodePDF_v2-Windows-${{ github.ref_name }}.zip
     shell: pwsh
   ```

   **新しいコード:**
   ```yaml
   - name: Create ZIP archive
     run: |
       # ファイルの存在を確認
       if (Test-Path "dist\BarcodeQRCodePDF_v2.exe") {
         Write-Host "✓ Found BarcodeQRCodePDF_v2.exe"
         Compress-Archive -Path dist\BarcodeQRCodePDF_v2.exe, dist\README.txt -DestinationPath BarcodeQRCodePDF_v2-Windows-${{ github.ref_name }}.zip
       } elseif (Test-Path "dist\main_v2.exe") {
         Write-Host "✓ Found main_v2.exe (renaming)"
         Move-Item dist\main_v2.exe dist\BarcodeQRCodePDF_v2.exe
         Compress-Archive -Path dist\BarcodeQRCodePDF_v2.exe, dist\README.txt -DestinationPath BarcodeQRCodePDF_v2-Windows-${{ github.ref_name }}.zip
       } else {
         Write-Host "✗ No exe found"
         Get-ChildItem dist\
         exit 1
       }
     shell: pwsh
   ```

3. **「Create DMG」セクション（80行目付近）を以下に置き換え：**

   **現在のコード:**
   ```yaml
   - name: Create DMG
     run: |
       brew install create-dmg
       mkdir -p dist/dmg
       cp -r dist/BarcodeQRCodePDF_v2.app dist/dmg/
       create-dmg \
         --volname "BarcodeQRCodePDF_v2" \
         --window-pos 200 120 \
         --window-size 600 400 \
         --icon-size 100 \
         --app-drop-link 450 185 \
         "BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.dmg" \
         "dist/dmg/" || true
       
       if [ ! -f "BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.dmg" ]; then
         cd dist && zip -r ../BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.zip BarcodeQRCodePDF_v2.app
       fi
   ```

   **新しいコード:**
   ```yaml
   - name: Create DMG
     run: |
       # ファイルの存在を確認
       if [ -d "dist/BarcodeQRCodePDF_v2.app" ]; then
         echo "✓ Found BarcodeQRCodePDF_v2.app"
         APP_PATH="dist/BarcodeQRCodePDF_v2.app"
       elif [ -d "dist/main_v2.app" ]; then
         echo "✓ Found main_v2.app (renaming)"
         mv dist/main_v2.app dist/BarcodeQRCodePDF_v2.app
         APP_PATH="dist/BarcodeQRCodePDF_v2.app"
       else
         echo "✗ No .app found"
         ls -la dist/
         exit 1
       fi
       
       brew install create-dmg
       mkdir -p dist/dmg
       cp -r "$APP_PATH" dist/dmg/
       create-dmg \
         --volname "BarcodeQRCodePDF_v2" \
         --window-pos 200 120 \
         --window-size 600 400 \
         --icon-size 100 \
         --app-drop-link 450 185 \
         "BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.dmg" \
         "dist/dmg/" || true
       
       if [ ! -f "BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.dmg" ]; then
         echo "DMG failed, creating ZIP"
         cd dist && zip -r ../BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.zip BarcodeQRCodePDF_v2.app
       fi
   ```

4. **保存：**
   - コミットメッセージ: `fix: Add flexible file detection for v2.0.2`
   - **Commit directly to the main branch** を選択
   - **Commit changes** をクリック

---

### ステップ2️⃣: v2.0.2リリースを作成（3分）

1. **以下のURLをブラウザで開く：**
   ```
   https://github.com/seyaytua/barcode/releases/new
   ```

2. **フォームに入力：**

   **タグ（Choose a tag）:**
   ```
   v2.0.2
   ```

   **リリースタイトル（Release title）:**
   ```
   Release v2.0.2 - main_v2.py バーコード管理システム
   ```

   **説明（Describe this release）:**
   ```markdown
   ## リリース v2.0.2
   
   ### バグ修正
   - ✅ GitHub Actionsビルドの安定性を向上
   - ✅ 欠落していたcore/qrcode_generator.pyを追加
   - ✅ ビルドファイル名の柔軟な検出機能を追加
   
   ### 新機能（v2.0系）
   - 📊 main_v2.pyベースのバーコード管理システム
   - 👥 教職員と備品のタブ管理機能
   - 📅 日付指定機能
   - 📤 Excelエクスポート/インポート強化
   
   ### ダウンロード
   ビルドが完了するまで約10分お待ちください。
   完了後、以下のファイルがダウンロード可能になります：
   
   #### Windows版
   - `BarcodeQRCodePDF_v2-Windows-v2.0.2.zip`
   
   #### macOS版
   - `BarcodeQRCodePDF_v2-macOS-v2.0.2.dmg` または `.zip`
   
   ### システム要件
   - Windows 10以降 / macOS 10.14以降
   - メモリ: 2GB以上推奨
   
   ### 技術スタック
   - Python 3.11+
   - PySide6 6.6.0
   - python-barcode, qrcode, reportlab, openpyxl
   ```

3. **「Publish release」ボタンをクリック**

---

## 🎉 完了！

リリースを公開すると：

1. ⏳ **GitHub Actionsが自動起動**
2. 🔨 **Windows版ビルド開始**（約5-10分）
3. 🔨 **macOS版ビルド開始**（約5-10分）
4. ✅ **ビルド完了後、実行ファイルが自動追加**

### ビルド進捗確認：
```
https://github.com/seyaytua/barcode/actions
```

### 完成したリリースページ：
```
https://github.com/seyaytua/barcode/releases/tag/v2.0.2
```

---

## 📝 まとめ

**必要な作業：**

1. ✅ workflowの2箇所を更新（5分）
2. ✅ リリースページで「v2.0.2」を作成（3分）
3. ⏳ ビルド完了を待つ（10分）

**合計：約18分で完成！**

すべてブラウザ上で完結します！
