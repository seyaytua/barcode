# 🎯 v2.0.2リリース作成ガイド（超簡単版）

## ✅ 準備完了！

すべてのファイルがGitHubにプッシュ済みです：
- ✅ `barcode_v2.spec` - バージョン2.0.2に更新済み
- ✅ `version.py` - バージョン2.0.2に更新済み
- ✅ `version_info_v2.txt` - バージョン2.0.2に更新済み
- ✅ `core/qrcode_generator.py` - モジュール追加済み
- ✅ `RELEASE_v2.0.2.md` - リリースノート

---

## 📋 実施する手順（たった2ステップ！）

### ステップ1️⃣: Workflowファイルを修正（5分）

1. **以下のURLをブラウザで開く：**
   ```
   https://github.com/seyaytua/barcode/edit/main/.github/workflows/build.yml
   ```

2. **48-52行目付近の「Create ZIP archive」セクションを探して、以下で置き換える：**

   ```yaml
   - name: Create ZIP archive
     run: |
       # ファイルの存在を確認して柔軟に対応
       if (Test-Path "dist\BarcodeQRCodePDF_v2.exe") {
         Write-Host "✓ Found BarcodeQRCodePDF_v2.exe"
         Compress-Archive -Path dist\BarcodeQRCodePDF_v2.exe, dist\README.txt -DestinationPath BarcodeQRCodePDF_v2-Windows-${{ github.ref_name }}.zip
       } elseif (Test-Path "dist\main_v2.exe") {
         Write-Host "✓ Found main_v2.exe (renaming to BarcodeQRCodePDF_v2.exe)"
         Move-Item dist\main_v2.exe dist\BarcodeQRCodePDF_v2.exe
         Compress-Archive -Path dist\BarcodeQRCodePDF_v2.exe, dist\README.txt -DestinationPath BarcodeQRCodePDF_v2-Windows-${{ github.ref_name }}.zip
       } else {
         Write-Host "✗ No expected exe file found in dist/"
         Get-ChildItem dist\
         exit 1
       }
     shell: pwsh
   ```

3. **80-96行目付近の「Create DMG」セクションを探して、以下で置き換える：**

   ```yaml
   - name: Create DMG
     run: |
       # ファイルの存在を確認
       if [ -d "dist/BarcodeQRCodePDF_v2.app" ]; then
         echo "✓ Found BarcodeQRCodePDF_v2.app"
         APP_PATH="dist/BarcodeQRCodePDF_v2.app"
       elif [ -d "dist/main_v2.app" ]; then
         echo "✓ Found main_v2.app (renaming to BarcodeQRCodePDF_v2.app)"
         mv dist/main_v2.app dist/BarcodeQRCodePDF_v2.app
         APP_PATH="dist/BarcodeQRCodePDF_v2.app"
       else
         echo "✗ No .app bundle found in dist/"
         ls -la dist/
         exit 1
       fi
       
       # DMGを作成
       brew install create-dmg
       mkdir -p dist/dmg
       cp -r "$APP_PATH" dist/dmg/
       create-dmg \
         --volname "BarcodeQRCodePDF_v2" \
         --window-pos 200 120 \
         --window-size 600 400 \
         --icon-size 100 \
         --app-drop-link 450 185 \
         "BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.dmg" \
         "dist/dmg/" || true
       
       # create-dmgが失敗した場合は、ZIPを作成
       if [ ! -f "BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.dmg" ]; then
         echo "DMG creation failed, creating ZIP archive instead"
         cd dist && zip -r ../BarcodeQRCodePDF_v2-macOS-${{ github.ref_name }}.zip BarcodeQRCodePDF_v2.app
       fi
   ```

4. **保存：**
   - コミットメッセージ: `fix: Add flexible file detection for v2.0.2 build`
   - **Commit directly to the main branch** を選択
   - **Commit changes** をクリック

---

### ステップ2️⃣: v2.0.2リリースを作成（3分）

1. **以下のURLをブラウザで開く：**
   ```
   https://github.com/seyaytua/barcode/releases/new
   ```

2. **タグを入力：**
   - 「Choose a tag」に：**v2.0.2** と入力
   - 「Create new tag: v2.0.2 on publish」と表示されることを確認

3. **リリースタイトル：**
   ```
   Release v2.0.2 - ビルド修正版
   ```

4. **説明をコピペ：**
   ```markdown
   ## リリース v2.0.2
   
   ### バグ修正
   - ✅ GitHub Actionsビルドエラーを修正
   - ✅ PyInstallerファイル名検出の改善
   - ✅ 欠落していたモジュールを追加
   
   ### 新機能
   - main_v2.pyベースのバーコード管理システム
   - 教職員と備品のタブ管理機能
   - 日付指定機能の追加
   - Excelエクスポート/インポート機能の強化
   
   ### ダウンロード
   ビルドが完了するまでお待ちください（約10分）
   
   #### Windows版
   - `BarcodeQRCodePDF_v2-Windows-v2.0.2.zip`
   
   #### macOS版
   - `BarcodeQRCodePDF_v2-macOS-v2.0.2.dmg` または `.zip`
   
   ### システム要件
   - Windows 10以降 / macOS 10.14以降
   - メモリ: 2GB以上推奨
   ```

5. **「Publish release」ボタンをクリック**

---

## 🎉 完了！

リリースを公開すると：
- ⏳ GitHub Actionsが自動起動（約10分）
- ✅ Windows版 `BarcodeQRCodePDF_v2.exe` が自動ビルド
- ✅ macOS版 `BarcodeQRCodePDF_v2.app` が自動ビルド
- 🎊 実行ファイルがリリースページに自動追加

### ビルド進捗の確認：
```
https://github.com/seyaytua/barcode/actions
```

### 完成したリリースページ：
```
https://github.com/seyaytua/barcode/releases/tag/v2.0.2
```

---

## 📝 まとめ

**必要な作業：**
1. ✅ Workflowファイルを2箇所修正（5分）
2. ✅ リリースページで v2.0.2 を作成（3分）
3. ⏳ ビルド完了を待つ（10分）

**合計所要時間：約20分**

すべてブラウザ上で完結！gitコマンド不要！
